{
  "$schema": "https://oxc-project.github.io/oxc/schema/linter.json",
  // oxlint is the ONLY lint/format configuration in this repository.
  // There is deliberately no prettier, no biome and no .editorconfig.
  //
  // NOTE on the `Date.now()` ban: oxlint 0.12 does not implement
  // `no-restricted-syntax` or `no-restricted-properties`, and while
  // `no-restricted-globals` is listed by `oxlint --rules` it is not implemented
  // either (no checkmark). Verified empirically against oxlint 0.12.0: a file
  // containing `Date.now()` produces 0 diagnostics under all three rules.
  // `scripts/check-dependency-whitelist.ts`, which used to enforce this ban as
  // part of `pnpm check:deps`, was removed org-wide (DEPENDENCY_POLICY.md /
  // MIGRATION_RUNBOOK.md step 3): the Tier import boundary that script also
  // enforced is now `no-restricted-imports` below, but this repository has no
  // `@nerima-games/*` dependency to restrict (it is outside the 4-tier graph;
  // see docs/architecture.md), so there is nothing to add there either. The
  // `Date.now()` ban itself has no tooling replacement and is enforced by
  // review only, same as every other org standard oxlint cannot yet express.
  // When oxlint gains `no-restricted-syntax`, add the ban here.
  "env": {
    "es2022": true,
    "node": true
  },
  "settings": {
    "typescript": {
      "version": "5.9"
    }
  },
  "plugins": [],
  // Categories are deliberately "off": this repository enables individual
  // rules explicitly below, not whole categories. Turning a category "warn"
  // blanket-enables every rule oxlint tags with that category (far more than
  // the curated list below), which is not the intent — see the curated
  // "rules" block, organized by the same category names as section comments.
  "categories": {
    "correctness": "off",
    "suspicious": "off",
    "perf": "off",
    "style": "off",
    "restriction": "off"
  },
  "rules": {
    // TypeScript-specific rules
    // Prefer `type` over `interface` for pure data types (Schema.Struct pattern).
    "@typescript-eslint/consistent-type-definitions": ["warn", "type"],
    "@typescript-eslint/no-explicit-any": "off",
    "@typescript-eslint/no-unused-vars": "warn",
    "@typescript-eslint/no-floating-promises": "warn",
    "@typescript-eslint/no-misused-new": "warn",
    "@typescript-eslint/no-unnecessary-type-assertion": "warn",
    "@typescript-eslint/no-var-requires": "warn",
    "@typescript-eslint/prefer-enum-initializers": "warn",
    "@typescript-eslint/prefer-for-of": "warn",
    "@typescript-eslint/prefer-literal-enum-member": "warn",

    // Correctness
    "no-array-constructor": "warn",
    "no-empty": "warn",
    "no-eq-null": "warn",
    "no-extra-boolean-cast": "warn",
    "no-fallthrough": "warn",
    "no-irregular-whitespace": "warn",
    "no-self-compare": "warn",
    "no-throw-literal": "warn",
    "no-unreachable": "warn",
    "use-isnan": "warn",
    "valid-typeof": "warn",

    // Suspicious
    "no-console": "warn",
    "no-debugger": "warn",
    "no-empty-function": "warn",
    "no-empty-pattern": "warn",
    "no-eval": "error",
    "no-implied-eval": "error",
    "no-redeclare": "warn",
    "no-shadow": "warn",
    "no-unreachable-loop": "warn",
    "no-unused-private-class-members": "warn",

    // Performance
    "no-await-in-loop": "warn",
    "no-loop-func": "warn",
    "no-promise-executor-return": "warn",
    "no-sync": "warn",

    // Style
    //
    // The rest of this category's rules as configured for oxlint 0.12
    // (array-bracket-spacing, block-spacing, brace-style, camelcase,
    // comma-dangle, comma-spacing, comma-style, computed-property-spacing,
    // eol-last, func-call-spacing, indent, key-spacing, keyword-spacing,
    // linebreak-style, lines-between-class-members, no-mixed-spaces-and-tabs,
    // no-new-object, no-tabs, no-trailing-spaces, no-whitespace-before-property,
    // object-curly-spacing, one-var, operator-linebreak, padded-blocks,
    // quote-props, semi-spacing, space-before-blocks, space-infix-ops,
    // space-unary-ops, spaced-comment) were removed here: nixpkgs' oxlint
    // (currently 1.73.0, see flake.nix) hard-fails config parsing with
    // "Rule '<name>' not found in plugin 'eslint'" for every one of them —
    // they are gone from the 'eslint' plugin in this version, not merely
    // renamed (confirmed empirically: none of the removed names, nor an
    // obvious renamed variant, appear in `oxlint --rules` output). They were
    // all "warn"/formatting-only (no "error" among them save the two tab/space
    // ones), so nothing that used to hard-gate the build is lost; there is no
    // tooling replacement for them under this oxlint version.
    "no-array-method-this-argument": "warn",
    "no-bitwise": "warn",

    // Restriction
    "default-case": "warn",
    "default-case-last": "warn",
    "no-empty-character-class": "warn",
    // Declared for intent and for the day oxlint implements it; inert in 0.12.
    // The real enforcement lives in scripts/check-dependency-whitelist.ts.
    "no-restricted-globals": ["warn", "name", "open", "close", "event"],
    "no-restricted-imports": [
      "warn",
      {
        "name": "effect",
        "message": "Please use named imports from 'effect' package",
        "importNames": ["default"]
      }
    ],
    "prefer-template": "warn",

    // Disabled rules (conflict with project style)
    "@typescript-eslint/explicit-function-return-type": "off",
    "@typescript-eslint/explicit-module-boundary-types": "off",
    "@typescript-eslint/no-non-null-assertion": "off",
    "no-case-declarations": "off",
    "no-constant-condition": "off",
    "no-param-reassign": "off",
    "no-plusplus": "off"
  },
  "ignorePatterns": ["node_modules/**", "coverage/**", "dist/**", ".git/**", "*.config.ts", "docs/**"]
}
